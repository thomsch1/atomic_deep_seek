# Main Configuration File for Development Environment
# This file defines default values that can be overridden by environment variables

# Server Configuration (can be overridden by environment)
SERVER_HOST := 0.0.0.0
SERVER_PORT := 2024

# Frontend Configuration  
FRONTEND_HOST := localhost
FRONTEND_PORT := 5173
FRONTEND_PORT_RANGE := 5173-5183

# Frontend Proxy Target (can be overridden by environment)
VITE_API_TARGET := http://localhost:$(SERVER_PORT)

# Development Settings
DEV_TIMEOUT := 120